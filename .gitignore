# Stage 1 working data — source files stay local; only their hashes are recorded.
data/raw/
data/clean/

# Results are large and regenerable, EXCEPT the run report, which is the audit
# trail: it records the environment, the pre-registration state and which gate
# stopped the run. That belongs in version control.
results/**
!results/
!results/**/
!results/**/run_report.json
!results/**/block_ic.csv
!results/**/decile_table.csv
!results/**/quadrant_table.csv
!results/**/audit_summary.json
!results/**/event_day_table.csv

__pycache__/
*.pyc
.venv/
